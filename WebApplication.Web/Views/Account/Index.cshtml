@model User
@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>Welcome @Model.Name</p>

<p>
    <a asp-action="LogTime">Log Time</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                Date
            </th>
            <th>
                Location
            </th>
            <th>
                Description
            </th>
            <th>
                Hours
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (Hours hours in ViewBag.Hours)
        {
            <tr>
                <td>
                    @hours.Date
                </td>
                <td>
                    @Html.DisplayFor(modelItem => hours.Location)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => hours.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => hours.TimeInHours)
                </td>
            </tr>
        }
    </tbody>
</table>